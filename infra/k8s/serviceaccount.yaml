apiVersion: v1
kind: ServiceAccount
metadata:
  name: three-peaks-api
  namespace: three-peaks
  annotations:
    # Workload Identity. This is what lets the pod reach the uploads bucket with
    # no key file: Application Default Credentials picks up the impersonation.
    iam.gke.io/gcp-service-account: three-peaks-api@realm-construction.iam.gserviceaccount.com
